        ListNode* s1 = head;
        for (int i = 1; i < k; i++) {
            s1 = s1->next;
        }
            temp = temp->next;
            t++;
        while (temp) {
        ListNode* temp = head;
        int t = 0;
        if (!head || !head->next) return head;
    ListNode* swapNodes(ListNode* head, int k) {
public:
class Solution {
 */
 * };
        }
        ListNode* s2 = head;

