        ListNode* first = head;

        temp->next = nullptr;   // break list
        ListNode* second = temp->next;

        }
            temp = temp->next;
        for (int i = 1; i < n - k; i++) {
        temp = head;
        // split at (n-k)

        if (k == 0) return head;
        k %= n;

        }
            temp = temp->next;
            n++;
        while (temp) {

