        temp = head;
        for (int i = 0; i < t - n - 1; i++) temp = temp->next;
        }
            return newHead;
            ListNode* newHead = head->next;
        if (n == t) {
        }
            temp = temp->next;
            t++;
        while (temp) {
        ListNode* temp = head;
        int t = 0;
    ListNode* removeNthFromEnd(ListNode* head, int n) {
public:
class Solution {
 */
        temp->next = temp->next->next;
        return head;

