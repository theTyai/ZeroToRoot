        for(auto p : s) {
            int r = p.first;
            int c = p.second;

·‌·‌·‌·‌·‌·‌·‌·‌·‌·‌·‌·‌//·‌row·‌zero
            for(int j = 0; j < m; j++)
                matrix[r][j] = 0;

            // column zero
        }
            }
                }
                    s.insert({i, j});
                if(matrix[i][j] == 0) {
            for(int j = 0; j < m; j++) {
        for(int i = 0; i < n; i++) {
            for(int i = 0; i < n; i++)
                matrix[i][c] = 0;

