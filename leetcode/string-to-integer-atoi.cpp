            }
            else if (isdigit(c)) {
                started = true;
                t = t * 10 + (c - '0');
                started = true;
                sign = (c == '-') ? -1 : 1;
            if (!started && (c == '-' || c == '+')) {

            if (!started && c == ' ') continue;
        for (char c : s) {

        bool started = false;
        int sign = 1;
        long long t = 0;
    int myAtoi(string s) {
public:
class Solution {

